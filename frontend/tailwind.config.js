 /** @type {import('tailwindcss').Config} */

export default {
   content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
   theme: {
     extend: {},
   },
    
  
   // here we have abandoned the require() format and  are instead using 
 
 }